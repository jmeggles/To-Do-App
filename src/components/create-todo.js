import React, { Component } from "react";

// exporting this component to import where needed
export default class CreateTodo extends Component {
    render () {
        return (
            <div>
                <p>This is the Create To-Do Component!</p>
            </div>
        )
    }
}